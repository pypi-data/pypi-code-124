import unittest
import subprocess


from commandnotfound import report


class CommandNotFoundReportTest(unittest.TestCase):
    def test_simple(self):
        try:
            subprocess.run(["gitk"])
        except FileNotFoundError as e:
            report(e, exit=False)
